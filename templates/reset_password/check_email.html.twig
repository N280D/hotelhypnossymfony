{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe{% endblock %}

{% block body %}

{{include("partials/title_section.html.twig",{'nom':'Réinitialisation du mot de passe'})}}
    <div class="login_register_wrap section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-6 col-md-10">
                    <div class="login_wrap">
                        <div class="padding_eight_all bg-white">
                            <div class="heading_s1">
                                <h3>Reset Password</h3>
                            </div>
                            <div class='content'>
                            <p>
                                Si votre compte existe, alors un email vous sera envoyé contenant un lien de réinitalisation que vous pouvez utiliser pour réinitaliser.
                                Le lien expirera dans{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                            </p>
                                <p>Si vous ne recevez pas d'email merci de vérifier vos spam email ou <a href="{{ path('app_forgot_password_request') }}">Essayer de nouveau</a>.</p>
                            <div class="form-note text-center">Don't Have an Account? <a href="{{ path('app_register')}}">Sign up now</a></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    
    
    
{% endblock %}
